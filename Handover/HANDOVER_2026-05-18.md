# DRIP Investing — Handover 2026-05-18

**GitHub:** https://github.com/rootray/remi-inv
**Current Branch:** master
**Session Date:** 2026-05-18

---

## Quick State (read this first)
- Phase 1 (Foundation) is fully complete — all 6 components built and wired
- The FastAPI app boots with 5 live endpoints across auth and user management
- Alpaca credentials are encrypted at rest with Fernet before DB storage
- Phase 2 (Alpaca Integration) is the immediate next step — start with `alpaca-client`

---

## Completed This Session
- `db-session` — async SQLAlchemy engine, session factory, `get_db()` FastAPI dependency
- `db-models` — User, AlpacaCredential, Rule, ReinvestmentLog ORM models with enums and relationships
- `auth-register` — `POST /auth/register` with bcrypt password hashing and duplicate email check
- `auth-login` — `POST /auth/login` issuing signed JWT (HS256, configurable expiry)
- `auth-middleware` — `get_current_user` FastAPI dependency; validates JWT, loads User from DB
- `api-users` — `GET /users/me`, `PUT /users/me/credentials`, `GET /users/me/credentials`
- `backend/auth/schemas.py` — shared Pydantic schemas (RegisterRequest, UserResponse)
- `backend/requirements.txt` — all backend dependencies pinned

## Orchestration File State
`backend/main.py` imports and mounts: `register_router`, `login_router`, `users_router`. Auth middleware (`get_current_user`) is a dependency — not a mounted router. DB session and models are imported by individual components. Nothing is stubbed in Phase 1.

## Open Decisions / Unresolved Questions
- Fernet key (`CREDENTIAL_ENCRYPTION_KEY`) must be a valid 32-byte URL-safe base64 key — generate with `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`
- Database migrations not yet set up — consider Alembic before Phase 2 testing

## Next Steps (in order)
1. Begin Phase 2: build `alpaca-client` — authenticated Alpaca SDK instance per user (decrypts stored keys, returns `TradingClient`)
2. Then `alpaca-portfolio` — fetch holdings via SDK
3. Then `alpaca-dividends` — detect dividend activity from account history
4. Then `alpaca-orders` — place fractional reinvestment buy orders

## Files Modified This Session
- `backend/main.py` — orchestration, all Phase 1 routers mounted
- `backend/requirements.txt` — created
- `backend/db/session.py` — complete
- `backend/db/models.py` — complete
- `backend/auth/schemas.py` — created
- `backend/auth/register.py` — complete
- `backend/auth/login.py` — complete
- `backend/auth/middleware.py` — complete
- `backend/api/users.py` — complete
- `components.json` — all Phase 1 components marked complete
- `PROGRESS.md` — Phase 1 all ticked

## Notes for Next Session
- Use paper trading URL for all Alpaca dev: `https://paper-api.alpaca.markets`
- `alpaca-client` must call `_fernet.decrypt(credential.encrypted_api_key).decode()` to retrieve plaintext keys — Fernet instance is in `backend/api/users.py` and should be extracted to a shared utility when `alpaca-client` needs it
- `alpaca-py` SDK class for trading: `alpaca.trading.client.TradingClient(api_key, secret_key, paper=True)`
- No DB migrations tool in place yet — add Alembic before running against a real Postgres instance
