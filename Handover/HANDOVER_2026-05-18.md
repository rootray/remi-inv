# DRIP Investing — Handover 2026-05-18

**GitHub:** https://github.com/rootray/remi-inv
**Current Branch:** master
**Session Date:** 2026-05-18

---

## Quick State (read this first)
- All four phases are complete — backend (Phases 1–3) and frontend (Phase 4) are fully built
- The app cannot run yet: Alembic migrations are not set up and `frontend/` has no `node_modules`
- Single most important next action: set up Alembic and run the first migration to create the DB schema

---

## Completed This Session

### Phase 2 — Alpaca Integration
- `backend/crypto.py` — shared Fernet encrypt/decrypt (extracted from api-users)
- `backend/api/users.py` — updated to import from `backend.crypto`
- `backend/alpaca/client.py` — `get_trading_client(user, db)` decrypts credentials, returns `TradingClient`
- `backend/alpaca/portfolio.py` — `get_positions(client)` returns all Alpaca positions
- `backend/alpaca/dividends.py` — `get_recent_dividends(client, since)` returns DIV activities
- `backend/alpaca/orders.py` — `place_fractional_buy(client, symbol, notional)` submits notional market buy

### Phase 3 — DRIP Automation
- `backend/rules/engine.py` — `evaluate(rules, source_symbol, amount)` pure function, first matching rule wins
- `backend/jobs/drip_scheduler.py` — Celery beat task (hourly), polls dividends for all users, evaluates rules, places orders, logs results
- `backend/api/portfolio.py` — `GET /portfolio/positions`, `/portfolio/dividends`, `/portfolio/reinvestment-log`
- `backend/api/rules.py` — `GET/POST /rules`, `PATCH/DELETE /rules/{id}` with Pydantic validation

### Phase 4 — Frontend
- `frontend/package.json`, `next.config.ts`, `tsconfig.json`, `tailwind.config.ts`, `postcss.config.mjs` — Next.js 15 + Tailwind CSS project initialised
- `frontend/app/layout.tsx`, `app/globals.css`, `app/page.tsx` — root layout and redirect to /dashboard
- `frontend/lib/api.ts` — typed fetch wrapper for all backend endpoints; JWT in localStorage; 401 → /login
- `frontend/app/(auth)/login/page.tsx` — login form; stores token; redirects to /dashboard
- `frontend/app/(auth)/register/page.tsx` — register + auto-login; redirects to /dashboard
- `frontend/app/dashboard/page.tsx` — positions, 30-day dividends, reinvestment log (parallel load)
- `frontend/app/rules/page.tsx` — create rules (conditional fields), toggle active, delete (optimistic UI)

---

## Orchestration File State
`backend/main.py` mounts all routers: register, login, users, portfolio, rules. Phase 2–3 components (alpaca-*, rules-engine, drip-scheduler) are used as dependencies or Celery tasks — not mounted as routers. All components wired; nothing is stubbed.

## Open Decisions / Unresolved Questions
- **Alembic not set up** — the DB schema does not exist yet; no tables will exist until migrations are run
- **`frontend/node_modules` not installed** — run `cd frontend && npm install` before `npm run dev`
- Scheduler deduplication uses symbol + amount matching — a future improvement is adding an `alpaca_activity_id` column to `ReinvestmentLog` for exact deduplication

## Next Steps (in order)
1. Set up Alembic: `pip install alembic`, `alembic init alembic`, configure `alembic/env.py` to use the async engine and import all models, generate first migration, run `alembic upgrade head`
2. Run `cd frontend && npm install`
3. Start backend: `uvicorn backend.main:app --reload`
4. Start frontend: `cd frontend && npm run dev`
5. Start Celery worker: `celery -A backend.jobs.drip_scheduler.app worker --loglevel=info`
6. Start Celery beat: `celery -A backend.jobs.drip_scheduler.app beat --loglevel=info`

## Files Modified This Session
- `backend/crypto.py` — created
- `backend/api/users.py` — updated to use shared crypto module
- `backend/alpaca/client.py` — complete
- `backend/alpaca/portfolio.py` — complete
- `backend/alpaca/dividends.py` — complete
- `backend/alpaca/orders.py` — complete
- `backend/rules/engine.py` — complete
- `backend/jobs/drip_scheduler.py` — complete
- `backend/api/portfolio.py` — complete
- `backend/api/rules.py` — complete
- `backend/main.py` — all Phase 2–3 routers mounted and documented
- `frontend/package.json` — created
- `frontend/next.config.ts` — created
- `frontend/tsconfig.json` — created
- `frontend/tailwind.config.ts` — created
- `frontend/postcss.config.mjs` — created
- `frontend/app/layout.tsx` — created
- `frontend/app/globals.css` — created
- `frontend/app/page.tsx` — created
- `frontend/lib/api.ts` — complete
- `frontend/app/(auth)/login/page.tsx` — complete
- `frontend/app/(auth)/register/page.tsx` — complete
- `frontend/app/dashboard/page.tsx` — complete
- `frontend/app/rules/page.tsx` — complete
- `.env.example` — added NEXT_PUBLIC_API_URL
- `components.json` — all phases marked complete
- `PROGRESS.md` — all phases ticked

## Notes for Next Session
- Alembic env.py must import `backend.db.models` so all model metadata is loaded before `autogenerate` runs
- Use `create_async_engine` from `backend.db.session` in alembic env.py — see the SQLAlchemy async migration docs
- The Celery worker and beat scheduler must both be running for DRIP automation to work
- Paper trading URL: `https://paper-api.alpaca.markets` — use for all dev/test
- `CREDENTIAL_ENCRYPTION_KEY` must be a valid Fernet key: `python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"`
