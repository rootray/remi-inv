# DRIP Investing — Handover 2026-05-18

**GitHub:** https://github.com/rootray/remi-inv
**Current Branch:** master
**Session Date:** 2026-05-18

---

## Quick State (read this first)
- Project initialised from scratch this session — no code written yet, skeleton only
- PROJECT_BRIEF.md signed off: web app, Alpaca brokerage automation, general public users
- Full component architecture agreed (18 components across 4 phases)
- All skeleton files and folders created; no logic in any file
- GitHub push is the immediate next action (Step 5, awaiting user approval)

---

## Completed This Session
- PROJECT_BRIEF.md — project definition and tech stack sign-off
- components.json — full component manifest
- PROGRESS.md — phase breakdown
- .env.example — all required environment variables documented
- .gitignore — Python, Node, OS, IDE, Celery patterns
- .claude/settings.json — pre-approved safe git commands
- .github/workflows/ci.yml — CI scaffold for backend (pytest) and frontend (npm test)
- backend/ skeleton — main.py + all 13 backend component stubs
- frontend/ skeleton — Next.js App Router structure with 4 page stubs and api client stub

## Orchestration File State
`backend/main.py` exists as a stub comment only. Nothing is connected yet — all components are stubs with no imports or logic.

## Open Decisions / Unresolved Questions
- GitHub repo name to use for `gh repo create` (suggest: `drip-investing`)
- Whether to use Fernet (cryptography library) or a KMS for encrypting Alpaca API keys at rest

## Next Steps (in order)
1. Run `gh repo create drip-investing --public --source=. --remote=origin --push` (user must approve)
2. Start Phase 1: build `db-session` first (all other backend components depend on it)
3. Then `db-models` — define User, AlpacaCredential, Rule, ReinvestmentLog tables
4. Then auth components (register → login → middleware)

## Files Modified This Session
- `PROJECT_BRIEF.md` — created
- `PROGRESS.md` — created
- `components.json` — created
- `.env.example` — created
- `.gitignore` — created
- `.claude/settings.json` — created
- `.github/workflows/ci.yml` — created
- `backend/main.py` — created (stub)
- `backend/auth/register.py` — created (stub)
- `backend/auth/login.py` — created (stub)
- `backend/auth/middleware.py` — created (stub)
- `backend/alpaca/client.py` — created (stub)
- `backend/alpaca/portfolio.py` — created (stub)
- `backend/alpaca/dividends.py` — created (stub)
- `backend/alpaca/orders.py` — created (stub)
- `backend/rules/engine.py` — created (stub)
- `backend/jobs/drip_scheduler.py` — created (stub)
- `backend/api/users.py` — created (stub)
- `backend/api/portfolio.py` — created (stub)
- `backend/api/rules.py` — created (stub)
- `backend/db/models.py` — created (stub)
- `backend/db/session.py` — created (stub)
- `frontend/app/(auth)/login/page.tsx` — created (stub)
- `frontend/app/(auth)/register/page.tsx` — created (stub)
- `frontend/app/dashboard/page.tsx` — created (stub)
- `frontend/app/rules/page.tsx` — created (stub)
- `frontend/lib/api.ts` — created (stub)

## Notes for Next Session
- Use paper trading URL for Alpaca during all development: https://paper-api.alpaca.markets
- Alpaca API keys stored per-user must be encrypted at rest — do not store plaintext in DB
- Celery beat schedule should default to polling every 5 minutes (DRIP_POLL_INTERVAL_SECONDS=300)
- Start backend with: uvicorn backend.main:app --reload
- All components are stubs — Phase 1 (Foundation) is the correct starting point
